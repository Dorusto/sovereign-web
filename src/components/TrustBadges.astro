<div class="flex flex-wrap justify-center gap-6 text-sm text-gray-400 mt-12">
    {['Fără Taxe Lunare', 'Control Total', 'Datele Tale Acasă'].map(badge => (
        <div class="flex items-center gap-2 bg-white/5 px-4 py-2 rounded-full border border-white/10 shadow-sm">
            <span class="text-emerald-400">✓</span> {badge}
        </div>
    ))}
</div>
